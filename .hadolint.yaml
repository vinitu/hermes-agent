ignored:
  - DL3007   # Pinning is intentional — we always pin the hermes-agent base to an explicit version
  - DL3002   # Upstream s6-overlay image must start as root, then drops services to hermes
  - DL3008   # Pinning apt versions is impractical for a rolling Debian base
  - SC2015   # info-level; A && B || C pattern is intentionally used here
